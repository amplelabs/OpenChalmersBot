const DialogActions = require("../dialog-actions");
const mocha = require("mocha");
const chai = require("chai");
const should = chai.should();

// describe("tests the dialog actions", () => {
//   const existingSlots = { foo: "bar" };

//   DialogActions.delegate(existingSlots).should.be.equal({
//     dialogAction: { slots: existingSlots, type: "Delegate" }
//   });
// });
